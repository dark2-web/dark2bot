let handler = async (m, { conn, text }) => {
    // هنا نكتب رد البوت
    conn.reply(m.chat, '🚀 أهلاً ! أنا شغال الآن خذلك سنكرز 🍫', m)
}

// الكلمة التي يستجيب لها البوت (الأمر)
handler.command = ['test', 'تجربة'] 

export default handler
