export const command = {
    name: 'صراحة',
    category: 'تسلية',
    async execute(sock, from, msg, args) {
        const questions = [
            'هل كذبت كذبة كبيرة من قبل؟',
            'ما هو أكثر شيء تندم عليه؟',
            'هل أنت في علاقة حب حالياً؟',
            'من هو الشخص الذي لا ترفض له طلباً؟',
            'ما هي أسوأ ذكرى في حياتك؟',
            'هل سرقت شيئاً من قبل حتى لو كان بسيطاً؟'
        ];
        const randomQ = questions[Math.floor(Math.random() * questions.length)];

        await sock.sendMessage(from, { 
            text: `*🎤 لـعـبـة صـراحـة*\n\n*السؤال:* ${randomQ}` 
        });
    }
};

